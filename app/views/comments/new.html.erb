<%= form_with model: @comment, url: { action: :create },local: true do |f| %>


  <%= f.hidden_field :post_id, value: @post.id %>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
